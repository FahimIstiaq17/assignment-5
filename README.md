- 1️⃣ What is the difference between var, let, and const?
Var is a old way to declare variables
let is modern way to declare variables and can be updated but not re-declared in same scope.
const is also modrn way but it can not be re-declared or  updated.

- 2️⃣ What is the spread operator (...)?
The spread operator is used to expand and copy values from an array or object.

- 3️⃣ What is the difference between map(), filter(), and forEach()?
Map creates a new array by transforming each item.
Filter creates new arrayt with only matchig items.
forEach creates loops through items and no returns.

- 4️⃣ What is an arrow function?
arrow function is a shorter way to write a function in JavaScript and it's a short and clean way to write function.

- 5️⃣ What are template literals?
template literal are a modern way to write strings in JavaScript using backticks.


